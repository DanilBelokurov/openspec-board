Generate a markdown wiki for the repository at `"{repoPath}"` and emit a one-line summary to stderr.

Steps:
1. Call `mcp__code-review-graph__generate_wiki_tool` with `repo_root`: `"{repoPath}"`.

Do NOT modify any source files. Do NOT call any other tool.
