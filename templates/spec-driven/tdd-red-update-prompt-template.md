You are running the RED UPDATE phase of a TDD cycle for one service in a code repository.

A previous RED run wrote failing tests in the working tree of the code worktree:
  `{codeWorktreePath}`

The tasks file is at:
  `{tasksPath}`

REWRITE the failing tests in the working tree with consideration of these comments:
{comments}

Do NOT write production code. Do NOT commit. The user will commit after re-reviewing.

The Iron Law: NO PRODUCTION CODE IN THIS PHASE. You are rewriting failing tests only.
